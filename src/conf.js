const CONF = {
    mapWidth: 1280, // as in App.css
    mapHeight: 720, // as in App.css
    birdSize: 50, // as in App.css
    maxBottom: 670, // 720 - 50 (map - bird)
    velocity: 16,
    acceleration: 0.016,
    jumpDistance: -5,
    pipeWidth: 50, // as in App.css,
};

export default CONF;
