import React from 'react';
import '../App.css'

/**
 * Attributes in JSX
 *
 * We can add any attribute in JSX usign sting literals as attribute.
 * Note that class is a reserved word in JS, so to add a CSS class attribute to a JSX element we need to use
 * className notation
 */



const Day3 = () => <div className="App"></div>
export default Day3;
